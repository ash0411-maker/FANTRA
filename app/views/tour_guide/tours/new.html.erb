<div class = "container">
	<div class = "row">
		<h1>ツアー新規投稿</h1>
		<%= render 'errors', tour: @tour %>
		<div>
			<%= form_for(@tour, url: tour_guide_tours_path) do |f| %>
				<%= render 'tour_form', f: f %>
		    <% end %>
		</div>
	</div>
</div>

