<h1>ツアー内容編集画面</h1>
<%= render 'errors', tour: @tour %>
<div class = "container">
	<div class = "row">
		<%= form_with(model: @tour, url: tour_guide_guide_tour_path(current_guide, @tour)) do |f| %>
			<%= render 'tour_form', f: f %>
		<% end %>
	</div>
</div>