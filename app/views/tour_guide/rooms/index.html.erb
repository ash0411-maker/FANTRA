<div class = "container">
	<div class = "row">
		<div class = "col-xs-8 col-xs-offset-1">
			<h2>チャット一覧</h2>
			<table class = "table">
				<thead>
					<tr class = "active">
						<th class = "text-center">チャット相手</th>
						<th class = "text-center">チャットルーム</th>
					</tr>
				</thead>
				<tbody>
					<% @rooms.each do |room| %>
						<tr class = "text-center">
							<td><%= room.tourist.family_name %> <%= room.tourist.name %></td>
							<td><%= link_to "メッセージ確認", tour_guide_room_path(room), class: "btn btn-default" %></td>
						</tr>
					<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>